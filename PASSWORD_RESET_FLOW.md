# Password Reset Flow Diagram

## 📊 Complete User Journey

```
┌─────────────────────────────────────────────────────────────────────┐
│                         PASSWORD RESET FLOW                          │
└─────────────────────────────────────────────────────────────────────┘

┌─────────────┐
│   /login    │  User visits login page
└──────┬──────┘
       │
       │ Clicks "Forgot Password?" link
       ↓
┌──────────────────┐
│ /forgot-password │  Forgot Password Page
└─────────┬────────┘
          │
          │ Enters email
          │ Submits form
          ↓
    ┌─────────────┐
    │  Supabase   │  Sends reset email via Supabase Auth
    │   Server    │  Uses: resetPasswordForEmail()
    └──────┬──────┘
           │
           │ Email sent
           ↓
    ┌──────────────┐
    │ Email Client │  User receives email
    │ (Gmail, etc) │  with reset link
    └──────┬───────┘
           │
           │ Clicks reset link
           │ Link: /reset-password#access_token=...&type=recovery
           ↓
┌────────────────────┐
│  /reset-password   │  Reset Password Page
└─────────┬──────────┘
          │
          │ 1. Page loads
          │ 2. onAuthStateChange() listens
          │ 3. Detects PASSWORD_RECOVERY event
          │ 4. Validates token
          ↓
    ┌─────────────┐
    │Token Valid? │
    └──────┬──────┘
           │
    ┌──────┴──────┐
    │             │
    ├─YES         ├─NO
    │             │
    ↓             ↓
┌─────────┐   ┌──────────────┐
│  Show   │   │ Show Invalid │
│  Reset  │   │ Link Message │
│  Form   │   └──────────────┘
└────┬────┘          │
     │               │ "Back to Login" button
     │               ↓
     │         ┌──────────┐
     │         │  /login  │
     │         └──────────┘
     │
     │ User enters new password
     │ Confirms password
     │ Submits form
     ↓
┌─────────────┐
│  Validate   │  Client-side validation
│  Password   │  • Min 6 characters
└──────┬──────┘  • Passwords match
       │
       │ Validation passes
       ↓
┌──────────────┐
│   Supabase   │  Updates password
│    Server    │  Uses: updateUser({ password })
└──────┬───────┘
       │
       │ Success
       ↓
┌──────────────┐
│   Success    │  Shows "Password Updated!" message
│   Message    │  Countdown: 2 seconds
└──────┬───────┘
       │
       │ Auto-redirect
       ↓
┌──────────────┐
│   /login     │  User can login with new password
└──────────────┘
```

## 🔄 State Machine

```
┌──────────────────────────────────────────────────────────────┐
│              RESET PASSWORD PAGE STATES                       │
└──────────────────────────────────────────────────────────────┘

    Initial
       ↓
  ┌─────────┐
  │ LOADING │  Verifying recovery link...
  └────┬────┘
       │
  ┌────┴────┐
  │         │
YES│         │NO
  │         │
  ↓         ↓
┌─────┐  ┌─────────┐
│VALID│  │ INVALID │  Invalid or expired link
└──┬──┘  └─────────┘
   │          │
   │          └─→ Show error + "Back to Login"
   │
   │ User fills form
   │ Submits password
   ↓
┌─────────┐
│UPDATING │  Updating password...
└────┬────┘
     │
     │ Update successful
     ↓
┌─────────┐
│ SUCCESS │  Password updated! Redirecting...
└─────────┘
     │
     └─→ Auto-redirect to /login
```

## 🔐 Security Flow

```
┌────────────────────────────────────────────────────────────┐
│                    SECURITY LAYERS                          │
└────────────────────────────────────────────────────────────┘

┌─────────────────┐
│  Reset Request  │
└────────┬────────┘
         │
         ↓
    ┌─────────────────────┐
    │ Supabase generates  │  ✓ Secure token generation
    │ one-time token      │  ✓ Time-limited (1 hour)
    └──────────┬──────────┘
               │
               ↓
    ┌──────────────────────┐
    │ Token sent via email │  ✓ Secure channel
    │ only to user's email │  ✓ No password in email
    └──────────┬───────────┘
               │
               ↓
    ┌──────────────────────┐
    │ User clicks link     │  ✓ Token in URL hash
    │ Token in URL         │  ✓ Not sent to server
    └──────────┬───────────┘
               │
               ↓
    ┌──────────────────────┐
    │ Client validates     │  ✓ onAuthStateChange
    │ via Supabase         │  ✓ PASSWORD_RECOVERY event
    └──────────┬───────────┘
               │
          ┌────┴────┐
       VALID      INVALID
          │          │
          ↓          ↓
    ┌──────────┐  ┌────────────┐
    │ Show form│  │ Reject     │
    └────┬─────┘  └────────────┘
         │
         │ Password submitted
         ↓
    ┌──────────────────────┐
    │ Client validation    │  ✓ Min 6 chars
    │                      │  ✓ Confirmation match
    └──────────┬───────────┘
               │
               ↓
    ┌──────────────────────┐
    │ Supabase updates     │  ✓ Secure API call
    │ password             │  ✓ Token still valid
    └──────────┬───────────┘
               │
               ↓
    ┌──────────────────────┐
    │ Session cleared      │  ✓ Must login again
    │ Token invalidated    │  ✓ One-time use only
    └──────────────────────┘
```

## 🎯 Route Protection

```
┌────────────────────────────────────────────────────────────┐
│                  MIDDLEWARE ROUTING                         │
└────────────────────────────────────────────────────────────┘

    Request
       ↓
┌──────────────┐
│  middleware  │
└──────┬───────┘
       │
       │ Check pathname
       ↓
┌────────────────────────┐
│ Is public route?       │
│ • /                    │
│ • /login               │
│ • /signup              │
│ • /forgot-password  ✓  │  ← Added
│ • /reset-password   ✓  │  ← Added
└──────┬─────────────────┘
       │
  ┌────┴────┐
YES         NO
  │         │
  │         ↓
  │    ┌──────────────────┐
  │    │ Private route?   │
  │    │ • /feed          │
  │    │ • /messages      │
  │    │ • /profile       │
  │    └────┬─────────────┘
  │         │
  │    Check auth
  │         │
  │    ┌────┴────┐
  │  YES         NO
  │    │         │
  │    ↓         ↓
  │  Allow   Redirect
  │           to /login
  │
  ↓
Allow
```

## 📧 Email Flow

```
┌────────────────────────────────────────────────────────────┐
│                     EMAIL JOURNEY                           │
└────────────────────────────────────────────────────────────┘

User requests reset
       ↓
App calls Supabase
       ↓
┌──────────────────────┐
│   Supabase Server    │
│                      │
│ 1. Validate email    │
│ 2. Generate token    │
│ 3. Load template     │
│ 4. Inject token URL  │
└──────────┬───────────┘
           │
           ↓
    ┌────────────┐
    │   SMTP     │  Sends email
    │  Server    │
    └──────┬─────┘
           │
           ↓
    ┌─────────────┐
    │ Email Client│
    │ (Inbox)     │
    └──────┬──────┘
           │
           │ User clicks link
           │ Example URL:
           │ https://yourapp.com/reset-password
           │ #access_token=abc123...&type=recovery
           ↓
    ┌─────────────┐
    │   Browser   │  Navigates to reset page
    │             │  Token in URL hash (#)
    └─────────────┘
```

---

**Legend:**
- `┌─┐ └─┘` = Process/Page
- `→ ↓` = Flow direction
- `✓` = Security feature
- `←` = New addition
